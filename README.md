# ONNX Weight Quantization

Shrinks ONNX files by quantizing large float constants into eight bit equivalents, while leaving all calculations in floating point.

## What it does

Standard ONNX quantization converts
